[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "virsh-sandbox",
]

[tool.uv.sources]
virsh-sandbox = { path = "../virsh-sandbox-py", editable = true }
